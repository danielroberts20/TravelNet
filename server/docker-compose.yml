services:
  ingest:
    build: app
    container_name: pi_ingest
    env_file: .env
    volumes:
      - /home/dan/services/travel-ingest/data:/data         # persistent DB
      - ./app:/app                          # live code mount
    ports:
      - "8000:8000"
    restart: unless-stopped
