[tool.pytest.ini_options]
testpaths = ["server/tests"]
markers = [
    "slow: marks tests that intentionally wait (e.g. timeout verification) — deselect with -m 'not slow'",
]

[tool.setuptools]
packages = []
